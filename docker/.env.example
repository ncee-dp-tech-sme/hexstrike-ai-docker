# ==============================================================================
# HexStrike - Example environment overrides
# ==============================================================================
# Copy this file to ".env" (same folder as docker-compose.yml) and adjust values.
# All variables are optional; defaults are applied via ${VAR:-default} in compose.
# Do NOT commit your real secrets.
# ==============================================================================

# --- Clair API endpoint ---
# Default (local Clair bound on 127.0.0.1:8080):
# CLAIR_ADDR=http://127.0.0.1:8080
# Example external Clair:
# CLAIR_ADDR=http://clair.example.org:8080

# --- External PostgreSQL (optional) ---
# Leave empty if you use the local postgres service.
# PGHOST=db.example.org
# PGPORT=5432
# PGUSER=postgres
# PGPASSWORD=superpass

# --- Metasploit database URL (optional) ---
# If set, HexStrike can connect from msfconsole to this DB.
# MSF_DB_URL=postgresql://msf:msf@db.example.org:5432/msf
